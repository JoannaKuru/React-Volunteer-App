import React from 'react'
import './App.css';

// Ei käytössä
function Phone(props) {

    return (
        <p>{props.phone}</p>

    // if (props.volunteer.id === props.idShowPhone)
    //     return (
    //     <p>{props.phone}</p>
    // )

    // else {
    //     return(null)
    // }
    )
}

export default Phone;